# Requirements — Haunt v1 (MVP)

**Seat:** Product Manager / BA · **Date:** 2026-09-21 · **Version:** 1.0 (draft for sign-off)
**Source proposal:** [`proposals/haunt/proposal.md`](../../proposals/haunt/proposal.md) · **Governing decision:** [`decisions/2026-09-16-haunt-gate.md`](../../decisions/2026-09-16-haunt-gate.md), including the CEO decisions log **D1–D8** and Corrections **C1–C6**
**Read against:** `constitution.md` **v1.3** (read from disk 2026-09-21), `roles/pm-ba.md` as amended 2026-09-16, `pipeline/evidence-standard.md` v1.1 including *"Claims about our own rules"*.

**Status: prepares and flags; does not certify** (Constitution 6.1). Nothing here is a price, a release clearance, or a decision reserved to the CEO under Constitution 5.4: *"The following are never automated: kill/proceed decisions, spending real money, pricing changes, anything affecting user data policy, and release to real users. Agents prepare; the founder decides."*

**Template note.** This document uses `pipeline/templates/requirements.md` and keeps all four of its required elements — the in-scope table with QA-verifiable acceptance criteria and priorities, the explicit out-of-scope list, the promise line, and the scope-change log. It adds sections the template does not have (definitions, a block-to-criterion map, deferred scope with preconditions, negative findings, open questions, suggestions). The deviation is stated rather than made silently.

**Evidence.** Tagged per `pipeline/evidence-standard.md`. **Almost every source in this document is a Candour artifact read from disk on 2026-09-21**, and is cited by file and section rather than by link. Where a sibling artifact carries an external `[E]` retrieval, that retrieval is attributed to the seat and date that made it and **is not re-verified by me** — I retrieved no external source in producing this document, and I say so here rather than letting attributed citations read as mine. Every assertion about what the Constitution or a gate condition requires quotes the clause in the same passage. Where a statement is this seat's professional judgment rather than a requirement, it says so.

---

## 0. Status — what this document can and cannot do today

**It can be built from. It cannot be signed off today, and I am recording the reason rather than letting the omission pass silently.**

The decision record's own sequencing clause: *"`/build haunt` — PM/BA to produce requirements. Conditions 1–3 gate sign-off of that document; Conditions 4, 7, 8 and 10 are written into it."* Against that:

| Gate on sign-off | Status today | Who closes it |
|---|---|---|
| **Condition 1** — cost model re-derived on the CTO's hours | **Discharged.** `products/haunt/cost-sheet-v2.md` (CFO, 2026-09-20) supersedes `cost-sheet.md` in full and re-derives on the CTO's 2,090 h + 188 h spikes + 360 h/yr + 115 h/yr. | Closed |
| **Condition 2** — the venue-index spike runs | **Run, three times.** `venue-index-spike.md` (2026-09-18), `venue-index-remediation.md` (2026-09-20), `venue-index-nameseach.md` (2026-09-21). **It did not kill the product and it did not lift Block 4.** | Closed as a condition; see the next row |
| **Condition 3** — somebody installs Arc Timeline 4 | **Waived by the CEO, 2026-09-18** (Correction C4). The competitive facts in §2 travel as **vendor-origin**, as C4 requires. | Closed |
| **CTO Block 4** — build commencement on the venue index as specified | **Live. The CTO's ruling is being written in parallel at `products/haunt/block-4-ruling.md` and does not exist as I write.** §7.7 of this document is a reserved, empty slot for the criteria it may add. | CTO (only the CTO lifts a CTO block; only the CEO overrules one — Constitution 5.6) |
| **CFO standing block** (Condition 9.9, restated at `cost-sheet-v2.md` §16.1) — no Haunt price publishes under Article 3 without a complete cost sheet behind it | **Live, narrowed.** The CFO records it lifts *"the moment the period is published"*. The period is now fixed company-wide at three years by **D6**; the CFO's sheet is priced on a **five-year** life. §10.4 and §21.3. | CFO |
| **L1** — qualified human legal review (Constitution 6.1) | **Live, and it is a launch bar, not a requirements bar.** Condition 9.6. | CEO to commission |

**What follows from that.** This document is complete enough for the CTO and the Engineer to work from on everything except venue identity, and it is deliberately arranged so the Block 4 ruling slots into §7.7 without renumbering anything. **Requirements sign-off should follow the Block 4 ruling, not precede it.** I am not blocking build commencement — that block is the CTO's and it is already held; restating it here would be one seat repeating another's concern, which `roles/pm-ba.md` as amended forbids: *"Seats repeating one another's concern do not multiply into independent blocks."*

**The one thing I do hold and am exercising.** My charter's blocking scope is *"Build start without acceptance criteria; scope changes mid-build without a written trade-off."* This document is the discharge of the first limb. The second limb is live from the moment build starts, and §24 is where it will be exercised.

---

## 1. How to read this

**Requirement IDs** are area-prefixed and stable: `CAP-3`, `VEN-7`. They are the handle the Engineer, QA and any future scope-change record use. **IDs are never reused and never renumbered.** Reserved ranges are marked where a pending input will add criteria.

**Priority** has three values and they mean different things:

| Priority | Meaning |
|---|---|
| **BLOCKING** | Launch-blocking. Derived from a clause of the Constitution, a gate condition, a seat's declared would-be block, or a CEO decision under 5.4. **Cutting one of these is not a scope change; it is an overrule or an amendment**, and goes to the CEO in writing. |
| **MUST** | Required for v1. The product is not the product without it, but no external rule compels it. Cutting one is a scope change under §24 and needs a written trade-off. |
| **CUT-LINE** | Genuinely cuttable. Listed so the cut is a decision rather than a discovery. §20.2 collects them. |

**Acceptance criteria** are written so a QA engineer who was not in the room can execute them. Where a criterion is a *negative* ("never does X"), it names the test that demonstrates absence, because "we didn't build it" is not a test.

**Source** column cites the artifact and section that the requirement comes from, so a reader can check I did not invent it.

---

## 2. The product as decided, in one page

A private, on-device journal of the places you have been.

- **Platforms: iOS *and* Android at launch** (D1, CEO 2026-09-20). React Native with Expo, with **native capture layers in Swift and Kotlin** (CTO, `android-and-stack-note.md` §2.7).
- **Capture is automatic; the record is not.** The OS (or, on Android, a clustering layer Candour builds) proposes a visit; **nothing enters the timeline without the user confirming it** (`proposals/haunt/proposal.md`, "What we are explicitly NOT building").
- **Fully offline.** Venue names come from a bundled Overture-derived index. **No places API, no analytics, no telemetry SDK.** The only outbound traffic is an opt-in backup and platform crash reporting the user has already enabled at OS level.
- **The venue page is the product**, not a feature: every visit, your rating, your notes, and an aggregate verdict on a place (`proposals/haunt/ceo-product-inputs.md` §1).
- **Sessions, not days.** A night out is the unit, and sessions are **derived from visits and recomputable — never stored as immutable rows** (`ceo-product-inputs.md` §2.1).
- **Pricing: a subscription ladder — monthly, quarterly, yearly — plus a discounted pay-once tier, with a two-week full-feature trial and no permanent free tier** (Condition 9 as amended; C3.0).
- **On lapse the product becomes read-only: automatic capture stops, manual entry continues, nothing is ever hidden** (Condition 9.7, CEO 2026-09-19).
- **Haunt publishes no guaranteed support date, and says so where a buyer will see it** (D4, and Constitution v1.3 Article 4).

**The thing this product sells** is the capture engine and the maintained venue index — not access to the user's own words. Condition 9.7: *"What the subscription now sells is the capture engine, the maintained venue index and the published support commitment — not access to the user's own words."* **The third limb of that sentence is gone with D4**, so the subscription sells two things, and §10 is written accordingly.

---

## 3. Definitions written once, because a ratchet is waiting to lock them

`products/haunt/ux-note-pricing.md` §7 item 8 assigns this to me in terms: *"No definition of 'visible' exists for Haunt… Condition 8.2's ratchet will permanently lock whatever definition is written first. It has not been written, so the ratchet is currently waiting to lock a blank."* It is written here, affirmatively and exhaustively, before anything is built.

**DEF-1 — "Entry".** An entry is any user-authored or user-confirmed record: a **confirmed visit**, its **rating**, its **notes**, a **venue's user-set name**, a **user merge**, a **session override**, and a **home-location correction**. An unconfirmed capture candidate is **not** an entry — it is a proposal the user has not adopted (`ux-note-pricing.md` §5.2(a)).

**DEF-2 — "Visible".** An entry is *visible* when it appears, unfiltered and without qualification, in **all** of: the timeline; search results; the venue page's individual-visit list; the venue page's aggregates; session summaries; map pins where a map exists; the export; and any count or total the app displays. **This list is exhaustive and affirmative.** A surface added later is covered by it unless this document is amended in the open.

**In the MVP as decided, every entry is visible in every one of those places, in every entitlement state, for ever.** There is no window, no cap, and nothing hidden (Condition 9.7). DEF-2 exists so that if a read-cap is ever proposed, Condition 8.2's ratchet — *"the window is a published number, it may be lengthened, never shortened, and never tightened for users who already wrote under it… the ratchet binds the window's scope as well as its number"* — locks a definition written when nothing turned on it.

**DEF-3 — "Session".** A grouping of visits computed over the visit record. A session is **never a stored immutable row**. It is derived from (visits + user overrides + entitlement intervals + capture-gap records), and may be **memoised in a cache that is rebuildable from those four inputs and discarded on any algorithm change** (`subscription-sizing-note.md` §7.1).

**DEF-4 — "Venue".** A Candour-owned local row with a local `venue_id` that is never derived from any dataset identifier. The Overture GERS ID is an **attribute**, never a handle (`feasibility-note.md` §3; `venue-index-remediation.md` §11).

**DEF-5 — "Capture gap".** A recorded interval during which Haunt was not watching, for any reason: permission downgraded, foreground service killed, Doze-suspended past threshold, device off, or **capture not entitled**. A gap is an *observation*, stored durably, never derived after the fact (`android-and-stack-note.md` §1.6; `subscription-sizing-note.md` §3.2, §7.1).

**DEF-6 — "Entitlement interval".** A durable, time-stamped record of what the user was entitled to between two instants. Entitlement is **never a boolean**: the question the journal asks is *"was capture entitled at 23:40 on 14 March?"* (`subscription-sizing-note.md` §3.2).

**DEF-7 — The usage model.** The Engineer has asked three times, in three notes, for a usage model and recorded that no Candour artifact carries one (`venue-index-nameseach.md` §10.2 item 4). **I am supplying one so the venue-index bars have a denominator, and labelling it for what it is.**

> **[J, PM/BA]** A moderately social user records roughly **100 visits a year across roughly 40 distinct venues**, weighted towards a handful of locals, at least one of which is in a dense city centre.

**This is adopted from the Engineer's own assumption at `venue-index-remediation.md` §8.4, not independently derived.** Per the evidence standard's independence rule it is therefore **one source, not two**, and it must not be cited as corroborated. It is a working assumption whose only job is to make "95% single-identity" mean something a person can picture: at 95%, about two of forty venue pages are wrong in year one.

---

## 4. Capture — CAP

**What this area is.** The native layer that notices the user has stopped somewhere, on both platforms, and records the proposal without ever recording the user's journal into anything JavaScript touches.

| # | Requirement | Acceptance criteria (QA-verifiable) | Priority | Source |
|---|---|---|---|---|
| **CAP-1** | **No JavaScript in the visit-recording path.** The Swift and Kotlin modules receive the platform event, read entitlement, and write to SQLite directly. JavaScript reads later. | (a) Instrument the JS runtime to record whether Hermes started; kill the app, deliver a synthetic visit on each platform, assert the visit is in SQLite and Hermes never started. (b) Static check: the capture module's dependency graph contains no bridge call. | **BLOCKING** | CTO **Block 3**, `android-and-stack-note.md` §4, extended at `subscription-sizing-note.md` §3.3 |
| **CAP-2** | **iOS capture uses `startMonitoringVisits`**, handles the two-prompt `Always` authorization, and detects the silent downgrade when the second prompt is declined while the app is off screen. | (a) Decline the second prompt with the app backgrounded; assert the app records a capture gap (DEF-5) within one launch and renders it. (b) Deliver a `CLVisit` with no `departureDate`; assert the queue row says so in words and does not invent a range. | **BLOCKING** | `feasibility-note.md` §1.1; the honesty surface is Article 1.3 — *"Pricing, capability, and limitations are stated plainly. We say what a product cannot do."* |
| **CAP-3** | **Android capture runs a declared location foreground service** with `android:foregroundServiceType="location"`, Activity Recognition transitions as trigger, batched FLP for fixes, and stay clustering. The persistent notification is the honest surface, not a tax. | (a) Reboot the device; assert capture resumes without the app being opened. (b) Force-stop from the OEM task manager; assert a capture gap is recorded covering the dead interval. (c) The notification is tappable and states what is being watched. | **BLOCKING** | `android-and-stack-note.md` §1.6; D1 |
| **CAP-4** | **Capture gaps are first-class data** (DEF-5), on **both** platforms, rendered on the timeline in plain words. | (a) Induce a gap by each of: permission downgrade, service kill, device off, lapse of entitlement. Assert each produces one gap record with correct bounds, and one timeline surface reading e.g. *"Haunt wasn't running between 14:10 and 19:40. Anything you did then isn't here."* (b) Assert a gap is never silently absorbed into an adjacent session. | **BLOCKING** | `android-and-stack-note.md` §1.6 — the claim *"automatic"* is substantiable on Android **only** alongside this surface; Article 4 *"claims we cannot substantiate are claims we do not make"* |
| **CAP-5** | **Capture works under reduced accuracy.** A user who refuses precise location still gets visits; the candidate radius widens from `horizontalAccuracy` rather than assuming a tight fix, and the widening is stated in the list header in words. | (a) With precise location off, assert visits are still delivered and the candidate list header reads *"Precise location is off, so this list covers about 500 m."* (b) Assert the header text is in the accessibility tree, not a greyed visual. | **MUST** | `feasibility-note.md` §1.1; `ux-note.md` §2.2, §5.3 |
| **CAP-6** | **The app is fully usable with location denied outright, in every tier including a paid one.** Manual entry is not a fallback mode; it is required. | (a) Deny location at install; assert every function except automatic capture is reachable and the app never nags, never blocks a screen, and never re-prompts unprompted. (b) Revoke location on a paying subscriber; assert the same. | **BLOCKING** | App Store Review Guideline **5.1.2(i)** [E, retrieved by UX 2026-09-18 and re-confirmed by the CTO 2026-09-19, `subscription-sizing-note.md` §6.3; **not retrieved by me**]; the stronger reading is the CTO's at §6.3(b) |
| **CAP-7** | **Capture stops verifiably when it is not entitled**, and the teardown is idempotent and survives relaunch and restart. | (a) Expire a trial; assert `stopMonitoringVisits` / foreground-service stop actually executed, by asserting zero visit rows after the boundary across a relaunch and a reboot. (b) Assert no visit is captured-and-withheld. See **ENT-6**. | **BLOCKING** | `subscription-sizing-note.md` §6.4 D1; UX **B9** |
| **CAP-8** | **One notification only, and only for capture failure.** At most one local notification per authorization-loss *state transition*, never repeated for the same event, permanently dismissible, with the same message as a persistent in-app banner for users with notifications off. | (a) Trigger the same authorization loss five times without an intervening recovery; assert exactly one notification. (b) With notifications denied, assert the in-app banner carries the identical message. (c) Assert no other notification type exists in the build — enumerate every `UNNotificationRequest` / `NotificationCompat` construction site in CI. | **BLOCKING** | UX **B1**, `ux-note.md` §1.1; Article 4 — *"no engagement mechanics designed to exploit compulsion"* |
| **CAP-9** | **The CEO's confirmation-prompt carve-out (D3) is bounded by CAP-8.** D3 permits *"a notification may prompt confirmation"* where a venue match is uncertain. It is permitted only as **functional capture**, non-repeating, optional, and the confirmation surface must stand alone if notification permission is declined. | (a) Decline notification permission; assert every uncertain-match confirmation is still reachable and completable from the in-app queue. (b) Assert the uncertain-match notification never fires more than once per capture event. | **BLOCKING** | D3, *"Two constraints that travel with it"*; Condition **8.4** as clarified at C1-O11 — *"what 8.4 forbids is interruption and urgency"* |
| **CAP-10** | **The battery cost of capture is measured before any claim is made about it**, on both platforms, over at least two real weeks on at least a Pixel, a Samsung and a Xiaomi plus iOS hardware. | (a) A published measurement exists. (b) CI fails the release if store copy contains a battery claim and no measurement artifact is present. | **BLOCKING** | `android-and-stack-note.md` §1.7; `feasibility-note.md` §1.1; Article 4 — *"claims we cannot substantiate are claims we do not make"*. **This is also CTO Block 2's lifting condition and is not mine to lift.** |

**Reserved: CAP-11 … CAP-14** for criteria arising from the Block 2 Android device-matrix spike.

---

## 5. The confirm interaction — CONF

**This is the product's core interaction and its biggest usability risk** (`ux-note.md` §1). It is also where **D8** put three binding constraints from measurement, and where the twenty-row question has to be resolved rather than admired.

### 5.1 The resolution I am making as PM/BA, and the trade-off I am accepting

Two correct design goals collide. UX designed the confirm row *"for recognition at a glance"* and twenty rows of a dense high street is not a glance (`ux-note.md` §1.2). The Engineer measured that at a five-row list single-identity falls to 93.9% and the pre-registered bar fails, while at twenty reachable rows it passes (`venue-index-nameseach.md` §7.3). The Engineer flagged the conflict rather than resolving it and said so: *"it is the UX seat's and the PM/BA's to resolve."*

> **Resolution: a short list by default, with typing as the path to the long one. The requirement is that twenty rows are *reachable in one action*, not that twenty rows are *displayed*.**

This is the shape `venue-index-remediation.md` §9.4 sketched and the measurements at §7.3 support. **The trade-off I am accepting, stated so it is not discovered later:** a user who does not type sees five rows, and on the Engineer's §7.3 table that configuration alone scores 93.9% rather than 100%. The design therefore depends on the search field being obvious enough that a user reaches for it when the first five rows are wrong. **That dependency is a usability question nobody has tested** (`ux-note-pricing.md` §7 item 1), and it is the single thing I would spend the usability-testing money on first.

### 5.2 Requirements

| # | Requirement | Acceptance criteria (QA-verifiable) | Priority | Source |
|---|---|---|---|---|
| **CONF-1** | **The queue is a pull, not a push.** Opening the app *is* the confirmation surface. No badge by default, no count in the tab bar unless the user opts in, no "N unconfirmed" banner, no progress toward "all caught up". | (a) Accumulate 27 candidates; assert no badge, no count, no banner, no red, no completion meter anywhere. (b) Assert the app icon badge is never set. | **BLOCKING** | UX **B1**; `ux-note.md` §1.1, §1.3; Article 4 |
| **CONF-2** | **Nearest is offered as the default suggestion; where the match is uncertain or several venues overlap, the app shows the options rather than choosing.** | (a) Construct an unambiguous fix; assert one clear suggestion. (b) Construct a fix with two venues inside the error radius; assert options are shown and none is pre-selected. | **BLOCKING** | **D8**, CEO 2026-09-21 |
| **CONF-3** | **Never auto-select.** No candidate is pre-selected, pre-highlighted, pre-expanded, or positioned as the resting state. Confirming requires an explicit choice. | (a) On every confirm surface, assert zero rows carry a selected state on open. (b) Assert that pressing the primary action with no selection does nothing rather than accepting row 1. | **BLOCKING** | **D8** constraint 3; `ux-note.md` §2.2 — *"a pre-selected top candidate is a pre-ticked box"*, Article 4 *"no pre-ticked boxes"*; independently `venue-index-remediation.md` §7 — never auto-select is what converts a 57.5% wrong-first-row rate into a scroll |
| **CONF-4** | **Twenty candidate rows are reachable in at most one action from any confirm surface, and typing is that action.** | (a) From the confirm row, assert the search field is reachable in one action and that a query can surface at least 20 ranked rows. (b) Assert the default list length and the reachable length are both build constants recorded in the schema doc, so a later change is visible in a diff. | **BLOCKING** | `venue-index-nameseach.md` §7.3 condition 1 — *"At five rows the headline falls to 93.9% and P1 fails"*; **D8** constraint 1 |
| **CONF-5** | **Name search is prefix-based and incremental, never whole-string or exact.** Matching begins at the first character; four characters must retrieve. | (a) On a fixture of the Engineer's 44 divergently-named venues, assert a four-character prefix retrieves the correct row in the reachable list for at least the rate the harness reports. (b) Assert typing the full registered name never performs *worse* than the prefix in the shipped ranking. | **BLOCKING** | **D8** constraint 2; `venue-index-nameseach.md` §5.3 — full registered name **50.0%**, four characters **100%** on divergent names [E, measured by the Engineer 2026-09-21] |
| **CONF-6** | **Filter and manual search are always reachable**, and *"this venue isn't listed"* lives in the search field as a persistent affordance — as soon as the user types, the first row offered is *"Use 'The Eagle'"* — and is repeated at the end of the list. | (a) Assert the affordance is present on every confirm surface without scrolling. (b) Assert it is a labelled control, not an icon. | **BLOCKING** | **D8**; `ux-note.md` §2.2 |
| **CONF-7** | **One row, three facts: name, category in UK words, street plus distance in metres.** Confidence is never shown as a number. | (a) Assert `pub.tavern` never reaches the UI. (b) Assert no confidence score is rendered anywhere. (c) Assert distance is rendered as one accessible label — *"120 metres away"* — not a number and a unit in separate elements. | **MUST** | `ux-note.md` §2.2, §5.3 |
| **CONF-8** | **The generic-token dragnet is suppressed.** A result that matches the query **only** on a locality token or a trade token is ranked below, or excluded from, results that match a distinctive token. Distance and category are prominent. | (a) On the Engineer's fixture, type `ludlow`, `coffee`, `bar`, `manchester`; assert the result list does not present five unrelated venues as equal-confidence answers. (b) Assert the suppression rule is a named, testable function, not a ranking accident. | **MUST** | `venue-index-nameseach.md` §5.4, §8.2 — the hazard the Engineer flagged to UX and the PM/BA and explicitly did not resolve |
| **CONF-9** | **Near-miss confusability is a tested property, not an assumption.** The Engineer measured that on **3.6%** of typed queries the nearest returned row is a name-similar *wrong* venue (`Castle Tea Room` 5 m from `Castle Lodge Buttery`). | (a) QA runs a false-pick fixture: for each near-miss pair in the harness, assert both rows are simultaneously visible with disambiguating facts (street, distance, category) before either can be chosen. (b) Assert the false-pick rate is reported as a release metric, not merely tested. | **BLOCKING** | `venue-index-nameseach.md` §8.1 — the Engineer's own words: *"the false-pick rate is a testable acceptance criterion and should be one"* |
| **CONF-10** | **A previously-chosen venue is marked as such in the row** — *"you chose this here before"* — because a row that is first for a reason the user cannot see is a row the user cannot audit. | (a) Confirm venue V at a location; return; assert V is marked and that the marking is in the accessibility label. | **MUST** | `venue-index-remediation.md` §9.1 |
| **CONF-11** | **Sticky choice is a ranking input and never a selection.** Score contribution `+ log(1 + β·n)` over confirmations made within **150 m** of the query point, β ≈ 4. It never pre-selects, never collapses the list, never hides an alternative. | (a) Run the harness's "goes next door" fixture; assert recall of the correct neighbour at the reachable list length is within **2 pp** of the same build with the prior disabled. (b) Assert the 150 m gate exists and is a named constant. | **BLOCKING** | `venue-index-remediation.md` §7, §9.1; criterion **P3** |
| **CONF-12** | **Enough context in the row to make confirmation recognition rather than recall**: day and weekday, arrival and departure, dwell in plain words (*"about 2 hours"*), the street, and two or three candidates inline. | (a) Assert every element is present without opening anything. (b) Assert an open-ended visit reads *"we did not see you leave"* rather than a fabricated range. | **MUST** | `ux-note.md` §1.2 |
| **CONF-13** | **Bulk actions at session level and day level, of equal weight.** *"Confirm all"* and *"discard the rest"* are styled identically. Bulk discard is never the destructive or regretful choice. | (a) Assert identical type weight, colour and position for both. (b) Assert no confirm-shaming copy appears on discard. | **BLOCKING** | `ux-note.md` §1.3; `ux-note-pricing.md` §5.1; Article 4 *"no confirm-shaming"* |
| **CONF-14** | **Repeat visits to the same venue in one day collapse to one row with a count, expandable.** | Six candidates at one venue on one day render as one decision with a count; expanding shows six. | **MUST** | `ux-note.md` §1.3 |
| **CONF-15** | **Candidates expire silently on a published schedule** — default 30 days, user-adjustable, stated in onboarding. Expiry is never announced, never counted, and **is not changed by a trial ending or a lapse**. | (a) Age candidates past the window; assert they disappear with no message and no count. (b) Expire a trial; assert the candidate expiry rule is byte-identical before and after. | **BLOCKING** | `ux-note.md` §1.3; `ux-note-pricing.md` §5.2(a) item 3 — *"a rule that fires because a payment did not is a rule the user was never told about"* |
| **CONF-16** | **A single setting: "Do not ask me — I will add places myself."** Capture off, queue empty, product still works. | Toggle it; assert capture stops verifiably (as CAP-7), the queue is empty, and every other function works. | **MUST** | `ux-note.md` §1.3; App Store 5.1.2(i) path |
| **CONF-17** | **The note field is open on the confirm row.** No second screen, no title, no "add a note" button to press first. The rating is one tap and is never gated behind the note. **An entry with a venue and a date is a finished entry** — no empty-state copy, no completion meter, no greyed placeholder that looks like a task. | (a) Assert the note field accepts input without navigation. (b) Search the build's string table for "complete", "finish", "%" progress copy on entries; assert none. | **MUST** | `ux-note.md` §3.2 |
| **CONF-18** | **Voice dictation is a first-class input to the note field**, using the platform's own on-device dictation. No audio is stored and none leaves the device. | (a) Assert dictation is reachable from the note field in one action. (b) Assert no audio file is written to the app container at any point. | **CUT-LINE** | `ux-note.md` §3.2 — *"a five-word thought said outside a pub is more likely to survive than the same thought typed on a phone in the rain"* [J, UX] |

---

## 6. Sessions — SESS

**Specified together with the confirm queue, not separately.** `ceo-product-inputs.md` closes with that instruction and the CTO put hours behind it: *"Specifying segmentation and the confirm queue together rather than separately is worth roughly 0.5–1 week… It disappears if the PM/BA writes them as two requirements in two places"* (`subscription-sizing-note.md` §7.4). **CONF-13 and SESS-3 are the same data structure viewed twice, and the Engineer should build them as one workstream.**

| # | Requirement | Acceptance criteria (QA-verifiable) | Priority | Source |
|---|---|---|---|---|
| **SESS-1** | **Sessions are derived and recomputable — never stored as immutable rows** (DEF-3). Visits are the atomic record. A memoisation cache is permitted and must be rebuildable from (visits + overrides + entitlement intervals + gaps). | (a) **The cache-deletion test:** delete the session cache, recompute, and assert the user sees exactly what they saw before — **including every correction they ever made**. (b) Change the segmentation algorithm's parameters; assert existing history re-groups and no history is lost or duplicated. | **BLOCKING** | `ceo-product-inputs.md` §2.1 — *"the single most consequential line in this document"*; CTO confirmation and amendment at `subscription-sizing-note.md` §7.1 |
| **SESS-2** | **User overrides are constraints on segmentation, anchored to visits — not patches to a session.** An override says *"these two visits are the same night"* or *"these two are not"*, is a durable row, and survives recomputation. | (a) Split a night; change the algorithm; recompute; assert the split survives. (b) Merge two nights; run a refresh and a recompute; assert the merge survives. (c) Assert no override is stored against a session identifier. | **BLOCKING** | `ceo-product-inputs.md` §2.2; the design that makes it work is the CTO's at `subscription-sizing-note.md` §7.1(1) — *"specified any other way, this is where the bugs live for the life of the product"* |
| **SESS-3** | **The queue groups by session, with day as the fallback.** One row per session: the venue chain, start and end, dwell in plain words — *"Friday evening, about 4 hours, 3 places"* — expandable to individual visits. Where segmentation confidence is low, fall back to the day grouping. | (a) Construct a four-venue night; assert one queue row and one confirm decision. (b) Construct an ambiguous boundary; assert the day grouping is used and the low confidence is stated. | **MUST** | `ux-note-pricing.md` §5.1 |
| **SESS-4** | **Split and merge are reachable from the queue and from the timeline, and an override is visible as an override and reversible.** *"You split this night. Undo?"* remains available from the session, permanently. | (a) Perform a split; assert it is labelled as the user's and is undoable at any later date. (b) Assert the label is in the accessibility tree. | **MUST** | `ux-note-pricing.md` §5.1 |
| **SESS-5** | **Low confidence is stated, not guessed.** *"We weren't sure where this night ended"* rather than a silent boundary. | Force a low-confidence boundary; assert the statement renders and does not invent a time. | **MUST** | `ux-note.md` §1.2 — *"do not ask a question the data cannot support"*; Article 1.3 |
| **SESS-6** | **The home inference is visible, editable, removable, and can be none.** A Settings row lists the places Haunt treats as home, with a plain statement of why it derives them. The home location is a **user-editable row in the store**, not a value recomputed on the fly, and it lives in the override table. | (a) Assert the derived home appears in Settings with its reason in body text. (b) Edit it; recompute segmentation; assert the user's value survives. (c) Delete it; assert segmentation still functions and no home is re-derived without the user being told. (d) Assert the home row appears in the export. | **BLOCKING** | `ceo-product-inputs.md` §2.3; CTO addition at `subscription-sizing-note.md` §7.2; Constitution Article 4 — *"Collect the minimum data necessary, **state why**, and delete it when no longer needed"*, applied to a derived fact |
| **SESS-7** | **Entitlement and visibility are evaluated at session granularity, never at visit granularity. A session that began inside an entitlement is captured, presented and counted whole.** | (a) Construct a night beginning 23:00 on the last entitled day and ending 02:00 the next; assert the whole session is captured and presented whole. (b) Assert the session summary is not silently partial. | **BLOCKING** | `ux-note-pricing.md` §5.2(b); Condition **8.6** — *"Aggregate and derived views — venue pages, average ratings, visit counts, **session summaries** — compute over the **complete** set of the user's entries, or state plainly on their face that they are partial"* |
| **SESS-8** | **A session that straddles a capture gap or an entitlement boundary states its partiality on its face**, using the same gap surface as CAP-4 — not a second result set. | (a) Straddle a gap; assert the session renders the gap inline and the summary carries the partiality statement in body text. (b) Assert no second query path exists for partial sessions. | **BLOCKING** | `subscription-sizing-note.md` §3.2(b), §7.3; Condition 8.6 |
| **SESS-9** | **Entitlement intervals and capture gaps are durable rows** (DEF-5, DEF-6), readable by the native capture modules, because a session recomputed in 2031 must know whether capture was entitled in 2027. | (a) Assert the interval table is readable from Swift and Kotlin without the JS runtime (see CAP-1). (b) Delete and rebuild the session cache after a lapse-and-resubscribe cycle; assert partiality statements reproduce exactly. | **BLOCKING** | `subscription-sizing-note.md` §3.2(a), §3.3, §7.1(2)(3) |

---

## 7. Venue index and venue identity — VEN

**This is the area Block 4 is held over, and §7.7 is a reserved slot for whatever the CTO's ruling adds.** Everything in §7.1–§7.6 is independent of that ruling: it is either a schema rule the CTO already made an architectural constraint, or a measured constraint from D8 that the CEO has decided.

### 7.1 The index itself

| # | Requirement | Acceptance criteria (QA-verifiable) | Priority | Source |
|---|---|---|---|---|
| **VEN-1** | **The venue index is bundled, offline, and queried on-device. No places API, ever.** | (a) Run the full core loop in aeroplane mode; assert every venue function works. (b) CI fails the build if the dependency graph contains any HTTP client reachable from the venue layer. | **BLOCKING** | `proposals/haunt/proposal.md`; CTO **Block 1** — *"avoided, not lifted"*, and it re-engages on any architecture touching a places API |
| **VEN-2** | **Locality is taken from inside the Places theme, never from Divisions.** Divisions is ODbL and share-alike; joining it to Places to produce the shipped index would make the shipped index a derivative database Candour must publish under ODbL. | (a) The index build script is inspected in review; assert no Divisions, Base, Buildings or Transportation input. (b) The build script records its inputs in a manifest shipped with the index. | **BLOCKING** | `compliance-note.md` §2.5 — *"this belongs in the requirements document as a stated rule with the reason attached, not as tribal knowledge in one seat's head"* |
| **VEN-3** | **Index scope is the Engineer's scope B** — `food_and_drink` plus the 32-value out-of-hierarchy allow-list (`inn`, `casino`, `social_club`, `music_venue`, `theatre`, `bowling_alley`, `liquor_store`, …). **Scope C is not built.** | (a) Assert `inn`-classified pubs (`Church Inn`, `The Charlton Arms`) are present. (b) Assert the allow-list is a committed file, not a literal in code. | **MUST** | `venue-index-remediation.md` §4 — scope C *"makes the dense case worse"* and costs 78.7 MB |
| **VEN-4** | **The shipped index carries an FTS5 prefix name index and a grid-cell spatial column with a plain B-tree index** — not R-Tree, which is not documented as available in `expo-sqlite`. | (a) Assert prefix search returns in under the agreed interaction budget on the largest sample area on the slowest supported device. (b) Assert no R-Tree dependency. | **MUST** | `android-and-stack-note.md` §2.3; `venue-index-remediation.md` §12 |
| **VEN-5** | **The index's bundle cost is recorded and visible to the CFO and CTO before build**, because the component the Engineer recommends most strongly is also the one that costs the most bundle: scope A minimal 23.8 MB → scope B fully-specified **48.5 MB**, a **+104%** increase, roughly half of it the name index. | The requirements sign-off pack carries the figure. No running-cost consequence: the index is queried on-device, so marginal running cost is **£0**, which is how Constitution 1.5 (*"Cheap to run, cheap to buy"*) is satisfied here — structurally, not by discipline. | **MUST** | `venue-index-remediation.md` §12 |
| **VEN-6** | **Offline near-duplicate clustering is conservative and auditable** — within 60 m and name-similarity ≥ 0.70, shipped as a `cluster_id`. QA tests the **false-merge** rate directly, because a wrong automatic merge is worse than a duplicate: it silently fuses two real pubs and the user has no reason to suspect it. | (a) Assert the parameters are named constants recorded in the schema doc. (b) Run a false-merge fixture and report the rate as a release metric. | **MUST** | `venue-index-remediation.md` §9.2 |

### 7.2 The schema rules — the five criteria Block 4 limb (b) requires, plus AC-6

**Block 4 limb (b):** *"The five acceptance criteria at `venue-index-spike.md` §9.3 … are **in the requirements document and in the schema**, not in a backlog."* The Engineer restated them as testable criteria at `venue-index-remediation.md` §11 and said plainly: *"These are prepared, not accepted: limb (b) is satisfied when the PM/BA has written them and the schema implements them, not when I have drafted them."*

**They are accepted here, in the Engineer's own terms, with one addition of mine at VEN-13.** They are adopted, not re-derived — the Engineer's drafting is better than a paraphrase would be.

**Schema preconditions** (without these, none of VEN-7..13 is testable):

- Table **`venue`** — Candour-owned local row. `venue_id` (local, stable, **never derived from any dataset identifier**), `display_name`, `lat`, `lon`, `category`, `name_source` ∈ {`dataset`, `user`}, `gers_id` (nullable attribute), `cluster_id` (nullable attribute), `first_referenced_at` (nullable).
- Table **`venue_ref`** — the referenced-ness fact. **`venue.first_referenced_at IS NOT NULL` is the definition of "referenced" and it is set once and never cleared.**
- Table **`venue_merge`** — durable user merges: `(surviving_venue_id, absorbed_venue_id, merged_at)`.
- Table **`refresh_proposal`** — pending, reviewable changes from a refresh, with a `declined_at` column.

| # | Requirement | Acceptance criteria (QA-verifiable) | Priority | Source |
|---|---|---|---|---|
| **VEN-7** | **On first reference, the venue row is copied, not pointed at.** `display_name`, `lat`, `lon`, `category` are written into the `venue` row and `first_referenced_at` is set. **Rendering a venue page must not read the shipped index at all.** | Reference a venue; **delete the shipped index file**; assert the venue page renders identically, including name, position and category. | **BLOCKING** | `venue-index-remediation.md` §11 AC-1; Block 4(b) |
| **VEN-8** | **A refresh may add and update unreferenced rows. It may never write to a referenced row — enforced in the schema, not in application code.** A trigger or equivalent rejects any refresh-path `UPDATE` to `venue` where `first_referenced_at IS NOT NULL`. | Reference venue X; run a refresh whose payload renames, moves, re-categorises X and changes its GERS ID; assert X is **byte-identical** afterwards and that the write was **rejected rather than silently skipped**. | **BLOCKING** | ibid. AC-2 |
| **VEN-9** | **Changes to referenced rows are offered, never applied, and declining is the default.** Proposals are inert: **if the user never opens the review surface, nothing changes, ever, including across subsequent refreshes.** No proposal auto-applies after any period. Declining is recorded so the same change is not offered repeatedly. | Reference X; run two refreshes proposing a rename; never open the review surface; assert X unchanged and exactly one open proposal. Decline it; run a third refresh; assert no new proposal for the same change. | **BLOCKING** | ibid. AC-3 |
| **VEN-10** | **A user's rename always outranks the dataset, permanently and across refreshes.** Once `name_source = 'user'`, no refresh may alter `display_name` and **no refresh may even propose a name change** — the question has been answered. | Rename X; run a refresh carrying three successive different upstream names; assert `display_name` unchanged and **zero** name proposals raised across all three. | **BLOCKING** | ibid. AC-4; `ux-note.md` §2.3 |
| **VEN-11** | **User merges survive refreshes, including a refresh that re-splits the upstream records the merge joined.** The merge is a durable row, not a derived property of the index. Re-clustering at refresh time may never dissolve a user merge. | Merge A and B; run a refresh in which the upstream rows behind A and B are re-split into three records with new GERS IDs; assert one surviving venue, all visits attached, history intact. | **BLOCKING** | ibid. AC-5 |
| **VEN-12** | **Reconciliation may not join on the GERS ID alone.** Matching uses position and name in addition to `gers_id`; a changed or vanished `gers_id` on a referenced row raises a proposal, never an update. | Run a refresh in which **every** GERS ID in the payload is regenerated but names and positions are unchanged; assert **zero** duplicate venues created and **zero** referenced rows mutated. | **BLOCKING** | ibid. AC-6 — added by the Engineer on the re-retrieved Overture warning that a release one month before the one Haunt is built on *"re-matches the corpus… users should expect a one-time elevated level of GERS ID churn"* [E, retrieved by the Engineer 2026-09-20; **not retrieved by me**] |
| **VEN-13** | **Added by this seat: the refresh review surface is not a notification and is not a queue that accumulates pressure.** Proposals are found in Settings and on the venue page. There is no count, no badge, no prompt, and no "N venues need review". | (a) Accumulate 40 open proposals; assert no badge, no count, no notification anywhere. (b) Assert the surface is reachable in at most two taps from Settings. | **BLOCKING** | Article 4 — *"no engagement mechanics designed to exploit compulsion"*. **VEN-9 creates a pile of pending things; UX's B1 and Condition 8.4 forbid a pile from being used as pressure. Nobody had joined those two.** |

### 7.3 Merge, sticky choice, rename — the repair layer

**They are MVP scope, and they are the repair layer, not the identity layer.** The Engineer measured it: *"adding clustering to the as-specified design leaves Manchester at 0.0% single-identity. Sticky choice on its own gets it to 40.8%… Merge, sticky choice and rename are the repair layer. They cannot be the identity layer"* (`venue-index-remediation.md` §9). The identity layer is CONF-4 and CONF-5.

| # | Requirement | Acceptance criteria (QA-verifiable) | Priority | Source |
|---|---|---|---|---|
| **VEN-14** | **Merge is user-facing, reversible, and reachable from any venue's detail screen** — *"this is the same place as…"*. Visits, notes and ratings move. | (a) Merge; assert all visits, notes and ratings attach to the survivor. (b) Unmerge; assert the original state is restored exactly. | **BLOCKING** | `ux-note.md` §2.3; CTO accepted as scope at `android-and-stack-note.md` §3.5 |
| **VEN-15** | **Rename in place edits the Candour-owned row and does not create a new identity.** The renamed name is what name search matches against, alongside the dataset name. | (a) Rename; assert `venue_id` is unchanged and no second venue exists. (b) Search for the user's own word; assert the venue is retrieved. | **BLOCKING** | `venue-index-remediation.md` §9.3 — *"a rename that forked the identity would reintroduce the split it is meant to prevent"* |
| **VEN-16** | **Added by this seat: the product offers merge where it can see a probable duplicate, statically and without urgency.** Where two *Candour* venues within 100 m share a distinctive name token, the venue page carries a plain in-context line: *"This might be the same place as X. Merge them?"* No notification, no count, no badge, no repetition after dismissal. | (a) Create the duplicate condition; assert the line appears on the venue page only. (b) Dismiss it; assert it never reappears for that pair. (c) Assert no notification, badge or count is produced. | **MUST** | The repair layer is useless if the user never notices the damage. Permitted because Condition **8.4** as clarified at C1-O11 expressly allows *"static, in-context disclosure"* — *"what 8.4 forbids is interruption and urgency"*. **This is a new requirement from this seat; see §23.B2.** |

### 7.4 What the measurements bind, restated as requirements

The three D8 constraints are already written as CONF-3, CONF-4 and CONF-5 rather than duplicated here. **They are repeated in this list only as a pointer**, because a reader looking for "the venue-identity requirements" should not have to know they live in the confirm section:

- **Twenty reachable rows** → CONF-4
- **Prefix matching, not whole-string** → CONF-5
- **Never auto-select** → CONF-3
- **The prior must not evict the neighbour** → CONF-11
- **False-pick rate is tested** → CONF-9

### 7.5 The regression fixture

| # | Requirement | Acceptance criteria (QA-verifiable) | Priority | Source |
|---|---|---|---|---|
| **VEN-17** | **The venue-index harness is a permanent, committed regression fixture, not a spike artifact.** It lives in the product repository, re-runs in about fifteen minutes, and its outputs are diffable. | (a) A clean checkout re-runs the chain and reproduces the committed outputs. (b) **CI runs a `PYTHONHASHSEED` sweep**, because the Engineer found a committed script whose output moved by up to 2 pp between processes: *"a regression fixture that is not itself tested for reproducibility is not a fixture"*. | **BLOCKING** | CTO's **S1**; `venue-index-remediation.md` §1, §13; `venue-index-nameseach.md` §9. **Twice in four days a Candour measurement lived only in a session scratchpad.** |
| **VEN-18** | **Every change to the index build, the ranking, the clustering parameters or the search policy re-runs the fixture and publishes the P1–P4 numbers in the change record.** | Change a ranking constant; assert CI reports the four numbers and fails on an unexplained regression. | **MUST** | `venue-index-remediation.md` §8.4 — *"a bar that requires new instrumentation is a bar that gets deferred"* |

### 7.6 The refresh cadence, decided

| # | Requirement | Acceptance criteria | Priority | Source |
|---|---|---|---|---|
| **VEN-19** | **The bundled index is refreshed quarterly, not monthly**, inside app updates. | The release process documents the cadence; the index manifest carries the Overture release identifier and the build date. | **MUST** | CTO open question 6 assigned this to *"PM/BA and UX, on my recommendation"*: 60–100 h/yr versus 45, and **two thirds fewer opportunities for the refresh hazard to fire**. **Decided: quarterly.** The trade-off accepted is staleness — a venue that opens in January is not in the index until April, and CONF-6's "isn't listed" path carries that case, which it must do anyway. |

### 7.7 Additional acceptance criteria from the CTO's Block 4 ruling — **PENDING**

> **RESERVED AND DELIBERATELY EMPTY.** The CTO's ruling on Block 4 is being written in parallel at `products/haunt/block-4-ruling.md` and does not exist as this document is written. **It may add acceptance criteria around venue identity, and this is where they go.**
>
> **IDs `VEN-20` onward are reserved for it.** Nothing above needs renumbering when they arrive, and nothing above depends on the ruling's outcome — §7.1–§7.6 are schema rules the CTO has already made architectural constraints, plus the three D8 constraints the CEO has decided. **If the ruling lifts Block 4 on the identity-denominated bar, P1–P4 become release-gating metrics and belong here. If it does not lift, what belongs here is whatever the CTO requires instead.**
>
> **What this document does not do, and why.** CTO open question 5 assigns the spike's pass criterion to *"PM/BA with UX and QA, **before** the spike runs"*. The spike has now run three times. **I decline to set that bar retrospectively and §21.2 states the reason in full**: setting a bar after seeing the answer is not a test, and dressing one up as a pre-registration would be worse than leaving the gap visible.

---

## 8. The venue page — VPAGE

**`ceo-product-inputs.md` §1 argues this is the product rather than a feature, and I agree** — with one consequence stated up front that nobody has written down as a requirement: *a venue page needs repeat visits to be anything at all*, and a fourteen-day trial produces one or two (`ux-note-pricing.md` §5.3). **VPAGE-7 is my answer to that and it is the cheapest thing in this section.**

| # | Requirement | Acceptance criteria (QA-verifiable) | Priority | Source |
|---|---|---|---|---|
| **VPAGE-1** | **The venue is a first-class entity with its own identity, aggregate state and history** — not a label on a visit. | The schema carries `venue` as in §7.2; the venue page is reachable from a visit, from search, and from a list of venues. | **BLOCKING** | `ceo-product-inputs.md` §1 — *"deciding that after the schema exists is the expensive version"* |
| **VPAGE-2** | **The page shows: every visit, your rating on each, your notes, and an aggregate verdict.** | Create 11 visits with ratings; assert all 11 are listed with their ratings and notes, and the aggregate matches. | **BLOCKING** | `ceo-product-inputs.md` §1 |
| **VPAGE-3** | **Derived views never lie about the user's own life.** Aggregates compute over the **complete** set of entries, or state plainly **on their face** that they are partial — in body text, not a tooltip, not behind an info icon. | (a) Assert a venue page never shows *"3 visits, average 3.8"* when the true figures are 11 and 4.2. (b) Straddle a capture gap; assert the partiality statement is body text on the page. (c) Assert there is exactly **one** query path — no second, entitlement-filtered result set exists anywhere. | **BLOCKING** | Condition **8.6**, quoted at SESS-7; `ux-note-pricing.md` §5.2(c) |
| **VPAGE-4** | **The venue page is entitlement-blind.** The read path has no entitlement dependency: queries unfiltered, aggregates unpartitioned, export one code path. | Static check in CI: no entitlement read is reachable from any journal query. **This runs on every build, not at pre-release**, because *"once one is there the export diverges from the app and Condition 8.3 breaks silently"*. | **BLOCKING** | `subscription-sizing-note.md` §6.1 — *"requirements should say so as a positive constraint"*. It is said. |
| **VPAGE-5** | **Merge, rename and the duplicate hint are reachable from the venue page** (VEN-14, VEN-15, VEN-16). | As those criteria. | **BLOCKING** | `ux-note.md` §2.3 |
| **VPAGE-6** | **The aggregate is honest about small numbers.** Where a venue has fewer than three rated visits, the page states the count alongside any average rather than presenting a mean of one as a verdict. | Create one 5-star visit; assert the page does not read *"5.0"* unqualified. | **MUST** | Article 1.3 — *"Honest by default. Pricing, capability, and limitations are stated plainly."* A mean of n=1 is a limitation stated as a fact. **New from this seat; see §23.B1.** |
| **VPAGE-7** | **The page tells the user what it becomes.** On a venue with few visits, one static line in body text: *"Right now this is one visit. Come back over a year and it becomes the record of a place."* Not a prompt, not a nudge, not timed, not repeated, not dismissible-into-nothing — a statement of what the feature is for. | (a) Assert the line is static and identical on day 1 and day 100. (b) Assert it disappears once the venue has real history rather than persisting as a nag. | **MUST** | `ux-note-pricing.md` §5.3 — *"the only way a two-week window can speak for a feature that needs twelve months"*. UX asked for exactly this and called it cheap. |
| **VPAGE-8** | **Query performance holds over a store that grows for five years.** | Load a synthetic store at the DEF-7 usage model × 5 years (≈500 visits, ≈120 venues) and again at 10× that; assert the venue page and search meet the interaction budget on the slowest supported device. | **MUST** | `subscription-sizing-note.md` §7.4 E4 |
| **VPAGE-9** | **Every venue-page function is reachable from a list, never only from a map.** | QA completes the full core loop — confirm, choose venue, open venue, read, edit, rate, merge — with VoiceOver and with Voice Control, **screen untouched**. | **BLOCKING** | UX **B3**; WCAG 2.2 SC 1.1.1 (A), 2.5.1 (A), 2.5.7 (AA) [E, retrieved by UX 2026-09-18; **not retrieved by me**]; Constitution v1.3 Article 4 — *"Meet accessibility standards (**WCAG 2.2 AA** as the working baseline, read through **WCAG2ICT** where the product is native software rather than a web page…)"* |

---

## 9. Entitlement, trial and lapse — ENT

**The shape, settled:** a subscription ladder with a two-week full-feature trial and no permanent free tier (Condition 9 as amended), and **on lapse the product becomes read-only: automatic capture stops, everything already written stays visible, manual entry may continue, nothing is ever hidden** (Condition 9.7).

**What that does to Conditions 8.1–8.6, stated once so nobody re-derives it:** *"There is now no window to govern, so on the MVP as decided they bind nothing — with one exception that binds regardless and is the most valuable thing the whole exercise produced: **8.3, export must be complete and unconditional in every state.** The rest stands **prospectively**."* (Condition 9.7.) **8.3 is written as DATA-4 and it is BLOCKING and absolute.**

| # | Requirement | Acceptance criteria (QA-verifiable) | Priority | Source |
|---|---|---|---|---|
| **ENT-1** | **Entitlement is stored as intervals over time, never as a boolean** (DEF-6), and the interval table is part of the SQLite contract, written by the StoreKit/Billing layer and readable natively by the capture modules. | (a) Assert the capture module answers *"was capture entitled at 23:40 on 14 March?"* without the JS runtime. (b) Assert intervals survive reinstall-while-offline and are never inferred from a current-state query. | **BLOCKING** | `subscription-sizing-note.md` §3.2(a), §3.3 (Block 3 extended) |
| **ENT-2** | **The "unknown" entitlement state is conservative and honest.** A device that has never synced, or is offline with no cached interval, is treated as a stated unknown — never silently as unentitled and never silently as entitled. | Construct each of: never-synced, synced-then-offline, refunded, revoked, billing-retry, grace period. Assert each renders a correct, plain status and never silently loses capture. | **BLOCKING** | `subscription-sizing-note.md` §3.1, §3.4 A2; CTO open question 4 |
| **ENT-3** | **Trial: two weeks, full feature.** Everything written during the trial remains readable, editable, searchable and exportable **for ever, at no charge, in the unpaid state**. No hiding, no read-only entries, no watermark, no delay, no cap. | Run a trial to expiry with 63 entries; assert all 63 remain fully readable, editable, searchable, exportable, and countable afterwards. | **BLOCKING** | Condition 9 as amended; `ux-note-pricing.md` §2.2 |
| **ENT-4** | **Manual entry keeps working, permanently, in the unpaid state — and it is a first-class composer, not a text field in an empty state.** Date, time in, time out, venue (from the index or typed), notes, rating, and the session it belongs to. | (a) In the unpaid state, author a complete visit from nothing and assert it is indistinguishable from a confirmed one in every surface. (b) Assert the composer is reachable in one action from the timeline. | **BLOCKING** | Condition 9.7 — *"a lapsed user retains a free manual journal indefinitely"*; CTO at `subscription-sizing-note.md` §6.2 — *"a composer that is the whole of the free product cannot be a text field at the bottom of an empty state"* |
| **ENT-5** | **Nothing is deleted on lapse.** Not entries, not venue rows, not ratings, not user merges or renames, not session overrides, not the home correction. A user who subscribes eighteen months later finds their own corrections intact. | Lapse; wait; resubscribe; assert every override, merge, rename and home correction is byte-identical. | **BLOCKING** | `ux-note-pricing.md` §2.2 item 6 |
| **ENT-6** | **Capture does not continue after entitlement ends, and captured-but-withheld is forbidden.** | (a) After expiry, assert zero visit rows are written for any subsequent interval. (b) Assert no table holds post-expiry candidates. (c) Assert nothing is "offered back on payment". | **BLOCKING** | UX **B9**; Article 4 — *"Collect the minimum data necessary, state why, and delete it when no longer needed"*; the shape is three named patterns at once per `ux-note-pricing.md` §2.5 |
| **ENT-7** | **The unpaid state never requests `Always` location for a feature it cannot use.** The permission request is attached to the capture capability and is unreachable when that capability does not exist. | Static check: the `Always` request site is reachable only from the entitled capture path. Manual QA: run the whole unpaid product and assert the prompt never appears. | **BLOCKING** | App Store **5.1.2(i)** [E, attributed to UX 2026-09-18 and the CTO 2026-09-19]; Article 4 minimum data |
| **ENT-8** | **Location monitoring verifiably stops at expiry, the app says so plainly, and it tells the user how to take the permission back.** *"Haunt has stopped using your location. You can remove the permission in Settings → Privacy & Security → Location Services → Haunt."* | (a) Assert monitoring stops (as CAP-7). (b) Assert the revoke route is stated in words on the status surface. (c) Assert the Privacy screen shows the live state. | **BLOCKING** | `ux-note-pricing.md` §2.5; there is **no iOS API by which an app relinquishes its own authorization** [K, CTO, flagged unretrieved at `subscription-sizing-note.md` §12 item 3] — so the app can only stop, say so, and show the user the door |
| **ENT-9** | **The expiry moment: three states, exactly as specified, and nothing else.** **State 1** (day 1 → day 13): a single static line, unchanged, in the home header and Settings — *"Trial — automatic capture is on until 2 October. Everything you write is yours and stays after that; only the automatic part stops."* **State 2** (first launch after expiry): one in-context banner at the top of the queue, two **equal-weight** actions, **not a modal**, and *"Not now"* dismisses **permanently**. **State 3** (thereafter): a factual Settings row. | (a) Assert the State 1 wording, colour, position and size are identical on day 1 and day 13. (b) Assert State 2 is not a modal, not full-screen, not an interstitial, and never reappears after dismissal — including after seven days, after an update, and after a reinstall-with-restore. (c) Assert equal visual weight by measured type size and contrast. (d) Assert no count of "what is lost" appears, because nothing is lost. (e) Assert no red, no warning iconography, no *"your trial has expired"* framing, and no greyed-out styling on a fully functional journal. | **BLOCKING** | UX **B8**; `ux-note-pricing.md` §2.3 |
| **ENT-10** | **The charge-date disclosure rule, if the store route charges by silence.** Permitted: a static line stating the **date** and the **amount**, present unchanged from day 1. Forbidden: a decrementing count of days, a progress bar, a badge, a notification of any kind, a modal, anything that becomes more prominent as the date nears, and anything phrased as loss. | (a) Assert the disclosure is identical on day 1 and day 13. (b) Assert no string in the build matches *"days left"*, *"last chance"*, or a loss framing. | **BLOCKING** | `ux-note-pricing.md` §2.4. **The test UX proposed and I adopt: *"A statement of when money will be taken is disclosure. A statement of what you will lose, delivered to move you, is urgency."*** UX flagged that this is its reading and not a settled rule, and that **the seat that settles the scope of a gate condition is the CGO**. That question is open at §22 item 4. |
| **ENT-11** | **Trial state is stored in the ordinary app container and is lost on uninstall, by design and on the record. No covert persistent identifier is built.** No Keychain survival token, no `identifierForVendor` retention, no device-recognition scheme of any kind. | (a) Install, run a trial to expiry, delete, reinstall; assert the trial is available again and **assert nothing identifying survived**, by inspecting Keychain and shared storage. (b) CI fails the build if the trial layer references a cross-uninstall store. | **BLOCKING** | UX **B7** as extended 2026-09-18; `ux-note-pricing.md` §2.6 — *"the fix is the breach"*. The cost of being wrong is asymmetric: a few unconverted trials against the product's only claim. |
| **ENT-12** | **Nothing is interposed between the user and the platform's cancellation flow.** The in-app cancel control deep-links directly to the system Subscriptions screen, is no harder to find than the purchase control, and there is **no** "before you go" sheet, retention offer, discount interstitial, survey, or "are you sure". | (a) Assert exactly one navigation hop from the subscription screen to the platform flow. (b) Assert no intermediate screen exists in the build. | **BLOCKING** | UX **B10**; Article 4 — *"Make cancellation as easy as signup"* and *"no confirm-shaming"* |
| **ENT-13** | **Restore Purchases exists behind an explicit user action**, and never runs silently. | Assert the control exists, is labelled, and that no automatic restore fires on launch. | **MUST** | `subscription-sizing-note.md` §3.4 A4 |
| **ENT-14** | **QA fixtures exist for the whole entitlement boundary**: expiry, grace period, billing retry, revoke, refund, restore, reinstall-while-offline, and the export diff. | The fixture set runs in CI on every build. | **BLOCKING** | `subscription-sizing-note.md` §6.4 D5; `ux-note-pricing.md` §8 |

**Reserved: ENT-15 … ENT-17** for the trial mechanism, pending the CTO/CGO answer on whether Apple permits the 3.1.1 non-subscription trial to an app that also sells auto-renewable subscriptions (§22 item 2). **That answer decides whether ENT-10 is load-bearing or precautionary.** UX recommends Route B (a trial that does not auto-charge) and records the cost of its own recommendation: a non-auto-charging trial almost certainly converts worse, and *"the size of that gap is the CFO's to size, not mine"*.

---

## 10. Pricing presentation — PRICE

**What this section is and is not.** The price *level* and the pricing *shape* are Constitution 5.4 decisions and belong to the CEO; the numbers belong to the CFO under Article 2.1. **This section specifies only how the tiers are presented**, which is where Article 4's pricing and marketing clauses bite. Nothing below is a price.

**The tiers as decided:** monthly, quarterly, yearly, and a **discounted** pay-once tier (Condition 9 as amended; C3.0 — *"The tier is to be discounted on the same curve as the others rather than priced at exactly five years of monthly"*).

| # | Requirement | Acceptance criteria (QA-verifiable) | Priority | Source |
|---|---|---|---|---|
| **PRICE-1** | **Every tier's per-unit cost is visible in one view without arithmetic, scrolling or tapping**: amount charged, billing period, and per-month equivalent **to the penny, at the same type size**. | Screenshot the plans sheet at default and at the largest accessibility text size; assert all four tiers show all three figures, same type size, one view, no truncation. | **BLOCKING** | UX **B12**; Article 4 — *"Use plain-language pricing with no hidden fees, forced bundles, or drip pricing"* |
| **PRICE-2** | **No tier is pre-selected, pre-highlighted, pre-expanded, or positioned as the resting state.** Purchase requires an explicit tap on a tier and then an explicit tap on buy. | Assert zero tiers carry a selected state on open; assert the buy control is inert until a tier is chosen. | **BLOCKING** | UX **B12**; Article 4 — *"no pre-ticked boxes"* |
| **PRICE-3** | **Badges are arithmetic, never adjectival.** *"Cheapest per month"* is permitted because it is checkable against PRICE-1's table. *"Best value"*, *"Recommended"* and **any claim about other customers' behaviour** — *"Most popular"* — are forbidden. | (a) Assert every badge string maps to a computable fact in the table. (b) Assert no popularity or recommendation claim exists in the build. | **BLOCKING** | UX **B12**. **A claim about other customers is unavailable to this product by construction: there is no telemetry, so Candour cannot know it** — Article 4, *"claims we cannot substantiate are claims we do not make"* |
| **PRICE-4** | **Savings are computed only against the monthly tier**, stated as both a percentage and cash per year, **rounded down, never up**, and never against a notional or withdrawn price. | Assert each displayed saving re-derives from the table and is rounded down. | **BLOCKING** | UX **B12**, §3.2 |
| **PRICE-5** | **No "from £X" headline.** If a single headline price is used, it is the **monthly** price, because that is what a user gets by doing the obvious thing. | Assert no minimum-tier headline exists anywhere in store copy or in the app. | **BLOCKING** | UX §3.2 — *"that is the ladder's version of drip pricing"* |
| **PRICE-6** | **The pay-once tier is named "Pay once".** The words "lifetime", "forever" and "for life" do not appear in any store or in-app copy for it. | Search store metadata and the build's string table; assert zero occurrences. | **BLOCKING** | UX **B11**; C3.1 — DMCCA s.226(b) catches *"an overall presentation which is likely to deceive… even if the information it contains is true"* [E, retrieved by the CGO; **not retrieved by me**]; Article 1.3 |
| **PRICE-7** | **The crossover is stated in plain words at point of sale** — the tenure at which paying monthly overtakes paying once — because it is the one fact that decides the choice and no store will tell the customer. | Assert the sentence is present, in body text, on the plans screen, and that its figures re-derive from the published cost sheet. | **BLOCKING** | `ux-note-pricing.md` §3.2 and `cost-sheet-v2.md` §9 reached this independently from design and cost. **One requirement, two seats, not two independent blocks** (evidence standard's independence rule, applied to seats). |
| **PRICE-8** | **The pay-once tier's screen states that paying once does not participate in future price reductions.** One sentence, body text, alongside the step-down date. | Assert the sentence is present and names the mechanism: a subscriber's renewals drop automatically because the store gives Candour no option to preserve the old price; a pay-once buyer has no ongoing price to reduce. | **BLOCKING** | C3.2 — the asymmetry nobody had named; UX §3.4; `cost-sheet-v2.md` §9. **It is an unusual thing to print and it is the reason the tier can be sold honestly next to a subscription.** |
| **PRICE-9** | **The price-review cadence and the scheduled step-down are stated at point of sale and in the published cost sheet, in advance — never as a push notification.** | Assert the cadence sentence is present on the plans screen and in the cost sheet, and that the announcement mechanism is a static in-app status and release notes. | **BLOCKING** | Condition 9.5; UX §3.4; **B1** for the delivery channel |
| **PRICE-10** | **Haunt states, before first sale and on its own pricing page, that no support date is committed.** Plain language, body text, where a buyer will see it. | Assert the statement is present in the App Store description, on the in-app plans screen, and in the cost sheet. | **BLOCKING** | **D4**; Constitution **v1.3 Article 4** — *"State, before first sale and on the product's own pricing page, **either the date until which the product will be supported, or plainly that no support date is committed**. Silence is not an option."* And C5.2: *"Not saying it is a breach of Article 4."* |
| **PRICE-11** | **The amortisation period is never published as, or in place of, a support statement**, and the cost sheet carrying it says so on its face. | Assert the cost sheet carries the statement; assert no customer-facing surface presents "three years" as a support commitment. | **BLOCKING** | Constitution v1.3 Article 4, final sentence: *"A product's standard amortisation period (Definitions) is an accounting convention, is never published as or in place of a support date, and the cost sheet carrying it says so on its face."* |
| **PRICE-12** | **Haunt publishes the price-rise notice period it guarantees, before first sale**, on the pricing page and in the first cost sheet. The period may be lengthened, never shortened. Candour delivers that notice **by its own means** — a persistent, retainable in-app notice plus the pricing page — because no store notice period reaches it. | (a) Assert the guaranteed period is published in both places before any sale. (b) Assert the notice mechanism exists and is testable without a server. | **BLOCKING** | Constitution v1.3 Article 4 — *"Where a price rises for a customer who has already bought, give at least 90 days' notice before the new price applies… **Each product publishes the notice period it guarantees**, in its first cost sheet and on its pricing page, before its first sale; that period may be lengthened, never shortened."* Consequences at **C5.6**: Apple's own notice is **27 days**, *"so nothing in the store machinery will deliver Candour's 90"* [E, retrieved by the CGO 2026-09-21; **not retrieved by me**]. Owner named there: **PM/BA, checked by UX and the CGO.** |

### 10.4 The pricing copy cannot be finalised yet, and this is a finding rather than a gap

**Three drafted sentences in the record are written against a framework that has since changed, and two of them name a thing Haunt will not have.**

- `cost-sheet-v2.md` §9 drafts the crossover sentence as *"…covers the whole period we've committed to supporting Haunt — until [date]… We've committed to supporting Haunt until [date]."*
- UX's **B11** lifting condition reads: *"the tier is named 'Pay once'; **the published support date appears in body text on the same screen as the price**."*
- Condition 9.4 states the cliff as *"~32%… price must fall to ~68p"*; C5.4 restates it at three years as **30.3%, 99p → 69p**; `cost-sheet-v2.md` §10 computes **20.2%, 99p → 79p** on the v2 cost base at five years.

**D4 removed the support commitment. D6 fixed the amortisation period at three years company-wide. C5 executed both.** So:

1. **B11's lifting condition is unsatisfiable as written** — there is no published support date to put on the screen. **I am restating it, and §17 carries the restatement:** B11 lifts when the tier is named "Pay once" **and the Article 4 statement that no support date is committed appears in body text on the same screen as the price** (PRICE-6 + PRICE-10). Same protection, available framework. **This is a requirements-level correction to a UX lifting condition and the UX seat should confirm it; I am not editing that seat's note.**
2. **The crossover sentence (PRICE-7) cannot be drafted until the CFO restates the ladder on the three-year period.** The three-year prices exist in `cost-sheet-v2.md` §0-B (monthly 99p, quarterly £2.59, yearly £9.89, pay once **£28.99**), but §7.3's margin table, §9's crossover and §10's cliff are all computed on **five years**. **No document in this repository carries the cliff percentage at three years on the v2 cost base.**
3. **Therefore PRICE-7, PRICE-8 and PRICE-9 are specified as requirements and their copy is owed by the CFO.** Owner: **CFO**, before any price publishes under Article 3. This is not a new block; it is the existing CFO block (Condition 9.9) pointing at a specific missing artifact.

---

## 11. Notices and consumer duties — NOT

**Nothing in the DMCCA subscription regime is in force today; the Government's stated commencement is January 2027; the duties must be planned on as Candour's, not Apple's; and they are dischargeable on this architecture without collecting anything** (Condition 9.6; `subscription-compliance-note.md` §6.1) [E throughout, retrieved by the CGO 2026-09-18/19; **not retrieved by me**].

| # | Requirement | Acceptance criteria (QA-verifiable) | Priority | Source |
|---|---|---|---|---|
| **NOT-1** | **Pre-contract information mapped onto store metadata and the first-run disclosure screen, built once rather than twice.** | The mapping is a committed document; QA asserts every mapped item appears in both places. | **BLOCKING** | **R1**, `subscription-compliance-note.md` §6.3 |
| **NOT-2** | **A persistent, append-only in-app notices surface.** Never auto-clearing, never dismissible into nothing, **included in the export**. | (a) Write ten notices; assert none can be cleared and all persist across relaunch, update and restore. (b) Export; assert all ten are in the archive. | **BLOCKING** | **R2**; and it is included in the export because Condition **8.3** requires the export to contain every entry |
| **NOT-3** | **Reminder and cooling-off notices are written to the notices surface and paired with a local notification, driven by renewal dates read on-device. No email address, no account, no server.** | (a) Simulate a renewal date; assert the notice is written and one notification fires. (b) Deny notification permission; assert the notice is still written and still findable. | **BLOCKING** | **R3**; the *"addressed personally"* limb of s.280(a) is answered by the paired local notification per the CGO |
| **NOT-4** | **A plainly-labelled in-app cancellation control deep-linking to the platform's Manage Subscriptions, with no retention offer, no confirm-shaming, no friction.** | As **ENT-12**. | **BLOCKING** | **R4**; Article 4; Condition 8.4 |
| **NOT-5** | **The notification-authorization question is answered before R2/R3 are built**, and the design works when notification permission is declined. | The answer is recorded; QA asserts the declined path. | **BLOCKING** | **R5** and CTO open question 2 — Apple's `requestAuthorization(options:)` page returned 404 for the CTO and the claim is tagged **[K], not retrieved**. **Twice-flagged rule applies: it must be resolved before it anchors a third artifact.** |
| **NOT-6** | **The price-rise notice mechanism (PRICE-12) and the notices surface (NOT-2) are the same surface**, not two. | Assert one implementation serves both. | **MUST** | C5.6 + R2. **Two duties, one mechanism; nobody had joined them.** |

**Not in scope and not fixable by build: the proportionate refund duty.** `subscription-compliance-note.md` §5.4 and C3.6: *"Candour has no refund mechanism and no build creates one."* It is a **Constitution 6.1** question and a **launch bar (L1)**, not a requirement. §19.2.

---

## 12. The store, export, import and backup — DATA

| # | Requirement | Acceptance criteria (QA-verifiable) | Priority | Source |
|---|---|---|---|---|
| **DATA-1** | **One SQLite file, no opaque blobs.** Notes are text, ratings are numbers, timestamps are ISO-8601 with an explicit offset. **Nothing in the store requires Haunt to interpret it.** | Open the file with the `sqlite3` CLI; assert every user-authored field is legible without the app. | **BLOCKING** | `feasibility-note.md` §3 |
| **DATA-2** | **The SQLite file is a written contract between four consumers** — Swift, Kotlin, JavaScript and the store/billing layer — with a published schema spec, a named migration owner, WAL and one-writer discipline. | (a) The schema spec is committed and versioned. (b) A concurrency fixture exercises simultaneous native write and JS read. | **BLOCKING** | `android-and-stack-note.md` §2.2; extended to four consumers at `subscription-sizing-note.md` §3.3 |
| **DATA-3** | **The export is a lossless dump of the store, not a report**, as a single `.zip` containing `haunt-export.json` (canonical, round-trippable), `visits.csv` and `venues.csv` (spreadsheet-openable), and `README.txt` (plain-English field descriptions plus the licence texts of §13). | (a) Export, wipe, import, export again; assert the two archives are semantically identical. (b) Assert every user-authored field round-trips. | **BLOCKING** | `feasibility-note.md` §3; Article 4 — *"export in a usable, machine-readable format"* |
| **DATA-4** | **Export is complete and unconditional in every state — this is absolute and it is the clause the whole pricing analysis rests on.** Free, immediate, machine-readable, in every tier, in the trial, after expiry, after a lapse, with **no penalty or disadvantage for having exported**. **An export implemented against the visible set rather than the full set is the breach.** | **The diff test, run on every build and not at pre-release:** export from a state constructed to have every awkward property — lapsed entitlement, capture gaps, declined refresh proposals, merged venues, renamed venues, session overrides — and **diff the export against the complete local store**. Assert zero divergence. | **BLOCKING** | Condition **8.3**, which Condition 9.7 preserves as binding *"regardless"*; Constitution Article 4 — *"Let customers leave with their data: export in a usable, machine-readable format, available at any time, at no charge, and **with no penalty or disadvantage imposed for having exported**"*. **C2.8 records that weakening 8.3 revives Article 1.2's lock-in limb immediately and with it UX's B2.** |
| **DATA-5** | **Import of the same archive ships at MVP.** One serialiser, three jobs: export, restore, and migration between platforms. | Import an archive produced on the other platform; assert full fidelity. | **BLOCKING** | `feasibility-note.md` §3 — *"export without import is a gesture"* |
| **DATA-6** | **Backup is opt-in and off by default. Nothing leaves the device until the user chooses it, and the choice screen says exactly what leaves and in what form.** Candour holds no key. | (a) Fresh install; assert zero outbound connections in a network capture across the full core loop. (b) Assert the backup toggle defaults off. | **BLOCKING** | `feasibility-note.md` §4.2; the substantiable marketing claim at `proposals/haunt/proposal.md` (third correction, 2026-09-10) |
| **DATA-7** | **Losing the backup key never loses the journal.** The local store remains readable and exportable independent of the backup key. | Destroy the backup key; assert the local journal opens, renders and exports in full. | **BLOCKING** | UX **B6**; `feasibility-note.md` §4.2 — *"made an acceptance criterion rather than an intention"* |
| **DATA-8** | **The recovery-key step never requires transcription, retyping or recall, and paste always works.** Verification is *"paste it back, or confirm it is in your password manager"* — never *"type characters 4, 9 and 17"*. | (a) Assert paste is enabled in the key field. (b) Assert no character-position challenge exists. (c) Assert "Save to the platform password manager", "Print or save a PDF" and "Copy" are offered at **equal visual weight**, and that *"Not now"* **cancels setup** rather than proceeding with an unsaved key. | **BLOCKING** | UX **B13**, newly blockable under Constitution Article 4's **WCAG 2.2 AA** baseline — SC **3.3.8 Accessible Authentication (Minimum), AA** [E, retrieved by UX; **not retrieved by me**]; design at `ux-note.md` §4.2 |
| **DATA-9** | **The backup screen is a status, not a nag.** A permanent Settings row: last backup time, size, and *"Recovery key: saved"* or *"Recovery key: not confirmed"*. **No periodic reminder.** | Assert the row exists and that no recurring prompt about the recovery code is scheduled anywhere. | **BLOCKING** | UX disagreement 3 at `ux-note.md` §8, accepted by the CTO at `android-and-stack-note.md` §3.5; **B1** |
| **DATA-10** | **The circularity is stated out loud on the key screen:** if the key is saved to a password manager that syncs via the same cloud account as the backup, losing that account loses both. One sentence, plus the printed-copy recommendation. | Assert the sentence is present on the key screen. | **MUST** | `ux-note.md` §4.2 — *"Saying it costs nothing; discovering it costs everything"*; Article 1.3 |
| **DATA-11** | **Explicitly barred from the backup flow:** scare modals; confirm-shaming (*"Are you sure? Your memories will be lost forever"*); a pre-ticked "I understand"; a default-on backup; any styling that makes *"Not now"* look like the wrong answer. | Assert absence of each by inspection and by string-table search. | **BLOCKING** | `ux-note.md` §4.2; Article 4's named list |
| **DATA-12** | **"Delete my backup" is a launch requirement, and the backup-setup screen states what deleting the app does and does not remove.** | (a) Enable backup; delete it from inside the app; assert the remote copy is gone and the local journal is untouched. (b) Assert the setup screen carries the app-deletion statement. | **BLOCKING** | **Condition 7**; `compliance-note.md` §3.5 — *"ciphertext of a person's location history would sit in their iCloud indefinitely with no in-app way to remove it… a plain Article 4 minimum-data failure and a trivially avoidable one"*; Article 4 — *"delete it when no longer needed"* |
| **DATA-13** | **The diagnostic bundle can never contain journal content, enforced by construction with a build-failing test.** The diagnostics buffer is a separate logging channel the journal layer is architecturally unable to write into. No venue names, no coordinates, no note text, no ratings. The user sees the entire bundle as readable text before anything happens. | (a) CI fails the build if journal strings appear in the diagnostics channel. (b) Assert the bundle is displayed in full before any share sheet opens. | **BLOCKING** | `feasibility-note.md` §5 Layer 3; `proposals/haunt/proposal.md` |
| **DATA-14** | **Per-build source maps are retained for the life of every shipped version**, with a symbolication runbook. | The release process produces and archives the map; a runbook exists and has been exercised once. | **MUST** | `android-and-stack-note.md` §2.3 — a permanent operational obligation created by the React Native decision |

### 12.1 One architecture decision this section is waiting on

**DATA-12 assumes a Candour-initiated remote backup exists.** The CTO recommends at `android-and-stack-note.md` §2.5 that **the user-saved encrypted file be the *only* backup at MVP**, dropping the cloud module entirely — which *"drops a community native dependency, drops a native module, drops the CloudKit honesty caveat… and is more transparent"*.

**If that recommendation is taken, Condition 7's "Delete my backup" requirement changes shape and must not be silently dropped.** It would be discharged instead by: the file is the user's, in their own file storage, and the backup screen says in plain words where it is and how to delete it. **I am recording both branches rather than picking one, because the architecture choice is the CTO's.** Whichever branch is taken, the Condition 7 obligation is discharged visibly, in this document, at sign-off. **Owner: CTO, at architecture.**
